// network configuration
const network = {
  development: { provider: "" },
};

// token configuration
const tokenConfig = {};

// gas configuration

const gasConfig = {};

module.exports = {
  network,
  tokenConfig,
  gasConfig,
};
